{
    "scripts": {
        "dev": "npm run build && nodemon index.ts",
        "build": "ts-node dom/generate.ts",
        "start": "ts-node index.ts"
    },
    "devDependencies": {
        "@types/express": "^4.17.21"
    },
    "dependencies": {
        "fs": "^0.0.1-security",
        "nodemon": "^3.1.4"
    }
}